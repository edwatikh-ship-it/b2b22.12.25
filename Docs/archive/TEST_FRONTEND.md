# 🧪 Тестирование Frontend

## Быстрый чеклист

После запуска `start-dev.ps1` проверь:

### ✅ 1. Проверка доступности серверов

**Backend:**
- Открой: `http://localhost:8001/docs`
- Должна открыться Swagger UI с документацией API

**Frontend:**
- Открой: `http://localhost:3000/dashboard`
- Должен открыться Moderator Dashboard

### ✅ 2. Проверка подключения Frontend → Backend

1. Открой консоль браузера (F12 → Console)
2. Перейди на любую страницу дашборда
3. Проверь Network tab (F12 → Network):
   - Запросы должны идти на `http://localhost:8001`
   - Не должно быть ошибок типа "Failed to fetch" или CORS

### ✅ 3. Тестирование основных страниц

#### Dashboard (`/dashboard`)
- [ ] Страница загружается без ошибок
- [ ] Навигация работает
- [ ] Все ссылки кликабельны

#### Manual Parsing (`/manual-parsing`)
- [ ] Форма отображается
- [ ] Можно ввести keyword
- [ ] Можно выбрать depth и source
- [ ] При отправке запрос уходит на `/moderator/manual-parsing`
- [ ] Показывается результат или ошибка

#### Parsing Runs (`/parsing-runs`)
- [ ] Список загружается с `/moderator/parsing-runs`
- [ ] Отображаются статусы (queued, running, succeeded, failed)
- [ ] Можно кликнуть на run для деталей
- [ ] Даты отображаются корректно

#### Parsing Run Details (`/parsing-runs/[runId]`)
- [ ] Детали загружаются
- [ ] Показываются результаты (если есть)
- [ ] Показываются логи (если есть)
- [ ] Авто-обновление работает для running статусов

#### Domain Queue (`/domains/queue`)
- [ ] Список доменов загружается с `/moderator/pending-domains`
- [ ] Сортировка работает (totalhits, firstseenat, domain)
- [ ] Можно кликнуть на домен для деталей

#### Domain Decision (`/domains/queue/[domain]`)
- [ ] Детали домена загружаются
- [ ] Показываются URLs и hits
- [ ] Можно принять решение (supplier/reseller/blacklist/pending)
- [ ] Запрос уходит на `/moderator/domains/{domain}/decision`

#### Keywords (`/keywords`)
- [ ] Список ключевых слов загружается
- [ ] Поиск работает (debounced)
- [ ] Даты отображаются корректно

#### Suppliers (`/suppliers`)
- [ ] Список поставщиков загружается
- [ ] Поиск работает
- [ ] Можно кликнуть на supplier для деталей
- [ ] Детали загружаются с `/moderator/suppliers/{id}`

#### Blacklist (`/blacklist`)
- [ ] Список доменов загружается
- [ ] Можно добавить новый домен
- [ ] Можно удалить домен
- [ ] Показываются URLs и комментарии

### ✅ 4. Проверка обработки ошибок

#### Backend недоступен:
- [ ] Показывается понятное сообщение об ошибке
- [ ] Не падает весь интерфейс
- [ ] Можно повторить запрос

#### 404 ошибки:
- [ ] Показывается "Not found" сообщение
- [ ] Есть кнопка "Назад"

#### 500 ошибки:
- [ ] Показывается сообщение об ошибке сервера
- [ ] Не падает весь интерфейс

#### Parser Service недоступен (503):
- [ ] Показывается сообщение "Parser service unavailable"
- [ ] Можно продолжить работу с другими функциями

### ✅ 5. Проверка UI/UX

- [ ] Темная/светлая тема переключается
- [ ] Все статусы отображаются правильными цветами:
  - Green: supplier
  - Purple: reseller
  - Black: blacklist
  - Yellow: pending
- [ ] Таблицы адаптивны (на мобильных)
- [ ] Loading states показываются при загрузке
- [ ] Empty states показываются когда данных нет

### ✅ 6. Проверка производительности

- [ ] Страницы загружаются быстро (< 2 сек)
- [ ] Нет лишних запросов к API
- [ ] Авто-обновление не создает бесконечных запросов
- [ ] Debounce работает для поиска

---

## 🐛 Типичные проблемы

### Frontend не подключается к Backend

**Решение:**
1. Проверь файл `frontend/moderator-dashboard-ui/.env.local`:
   ```
   NEXT_PUBLIC_API_URL=http://localhost:8001
   ```

2. Если файла нет, создай его:
   ```powershell
   cd frontend/moderator-dashboard-ui
   echo "NEXT_PUBLIC_API_URL=http://localhost:8001" > .env.local
   ```

3. Перезапусти frontend (Ctrl+C и снова `npm run dev`)

### CORS ошибки

**Решение:**
Проверь `backend/app/main.py` - должны быть настроены CORS:
```python
from fastapi.middleware.cors import CORSMiddleware
app.add_middleware(CORSMiddleware, allow_origins=["http://localhost:3000"])
```

### Порт занят

**Решение:**
```powershell
# Проверь порт 8001
netstat -ano | findstr :8001

# Если занят, останови процесс или измени порт
```

---

## 📊 Чеклист для коммита

Перед коммитом убедись что:
- [ ] Все страницы работают
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в логах backend
- [ ] Тесты проходят (`just test`)
- [ ] Линтер проходит (`npm run lint` в frontend)

