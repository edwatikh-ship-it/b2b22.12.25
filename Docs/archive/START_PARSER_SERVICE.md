# 🚀 Запуск Parser Service

## Проблема

Если парсер не запускается и статус остается "Queued", скорее всего **Parser Service не запущен**.

## Решение

### Способ 1: Запуск вручную

```powershell
cd D:\b2b\parser_service
python api.py
```

Или через uvicorn:
```powershell
cd D:\b2b\parser_service
uvicorn api:app --host 0.0.0.0 --port 9003
```

### Способ 2: Добавить в start-dev.ps1

Parser Service должен запускаться автоматически вместе с Backend и Frontend.

---

## Проверка работы

После запуска проверь:

```powershell
# Проверь порт 9003
netstat -ano | findstr :9003

# Проверь health endpoint
curl http://127.0.0.1:9003/health
```

Должен вернуться:
```json
{"status": "ok", "cdp_endpoint": "..."}
```

---

## Конфигурация

Parser Service должен быть доступен на:
- **URL**: `http://127.0.0.1:9003`
- **Health**: `http://127.0.0.1:9003/health`
- **Parse**: `POST http://127.0.0.1:9003/parse`

Проверь `backend/.env`:
```
PARSER_SERVICE_URL=http://127.0.0.1:9003
```

---

## После запуска Parser Service

1. Перезапусти Backend (если он уже запущен)
2. Попробуй запустить manual parsing снова
3. Статус должен измениться с "Queued" на "Running"

